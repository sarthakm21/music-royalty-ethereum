import React from "react";

const Login = () => {
  const handleClick = (e) => {};

  return (
    <div>
      <button onClick={handleClick}>Login with MetaMask</button>
    </div>
  );
};

export default Login;
